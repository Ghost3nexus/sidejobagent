app = "sidejobagent-api"
primary_region = "nrt"

[build]
  dockerfile = "backend/api/Dockerfile"

[http_service]
  internal_port = 8000
  force_https = true
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]

[env]
  PORT = "8000"
